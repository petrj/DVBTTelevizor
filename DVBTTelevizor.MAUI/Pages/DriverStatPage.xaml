<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DVBTTelevizor.MAUI.DriverStatPage"
             xmlns:local="clr-namespace:DVBTTelevizor.MAUI"
             BackgroundColor="Black"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             Title="{local:LngXamlExt Input='Driver statistics'}">

    <ContentPage.Resources>
        <ResourceDictionary>

            <Style x:Key="SeparatorBoxViewStyle" TargetType="BoxView">
                <Setter Property="VerticalOptions" Value="FillAndExpand"/>
                <Setter Property="HorizontalOptions" Value="FillAndExpand"/>
                <Setter Property="HeightRequest" Value="1"/>
                <Setter Property="Color" Value="Gray"/>
            </Style>
            <Style x:Key="StatLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="white"/>
            </Style>
            <Style x:Key="StatCaptionStyle" TargetType="Label">
                <Setter Property="TextColor" Value="#41b3ff"/>
            </Style>
            <Style x:Key="ButtonStyle" TargetType="Button">
                <Setter Property="Margin" Value="5,0,5,0"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="HorizontalOptions" Value="FillAndExpand"/>
                <Setter Property="VerticalOptions" Value="FillAndExpand"/>
                <!--Setter Property="Background" Value="#444444"/-->
                <Setter Property="CornerRadius" Value="7"/>
            </Style>

            <!-- Converter -->
            <local:StatValueConverter x:Key="ValueConverter" />

            <!-- TEXT TEMPLATE -->
            <DataTemplate x:Key="TextTemplate">
                <Grid ColumnDefinitions="60*,25*,15*" Padding="5"  ColumnSpacing="12">
                    <Label Text="{Binding Title}" Grid.Column="0" FontSize="{Binding FontSizeForLabel}" Style="{DynamicResource StatCaptionStyle}"/>
                    <Label Text="{Binding Value, Converter={StaticResource ValueConverter}}" Grid.Column="1"
                           HorizontalOptions="End" HorizontalTextAlignment="End" FontSize="{Binding FontSizeForLabel}"  Style="{DynamicResource StatLabelStyle}" />
                    <Label Text="{Binding Unit}" Grid.Column="2" HorizontalTextAlignment="Start" FontSize="{Binding FontSizeForLabel}"  Style="{DynamicResource StatLabelStyle}"/>
                </Grid>
            </DataTemplate>

            <!-- BOOL TEMPLATE -->
            <DataTemplate x:Key="BoolTemplate">
                <Grid ColumnDefinitions="2*,2*,Auto" Padding="5"  ColumnSpacing="12">
                    <Label Text="{Binding Title}" Grid.Column="0" FontSize="{Binding FontSizeForLabel}"  Style="{DynamicResource StatCaptionStyle}"/>
                    <CheckBox IsChecked="{Binding Value}" Grid.Column="1" HorizontalOptions="End" />
                    <Label Text="{Binding Unit}" Grid.Column="2" HorizontalTextAlignment="Start" FontSize="{Binding FontSizeForLabel}"  Style="{DynamicResource StatLabelStyle}"/>
                </Grid>
            </DataTemplate>


            <!-- TABLE TEMPLATE -->

            <DataTemplate x:Key="TableTemplate">
                <VerticalStackLayout Padding="5" Spacing="4">

                    <!-- Title -->
                    <Label Text="{Binding Title}" FontAttributes="Bold"  FontSize="{Binding FontSizeForLabel}"  Style="{DynamicResource StatCaptionStyle}"/>

                    <!-- HEADER -->
                    <Grid ColumnDefinitions="*,*,*,*"
                          ColumnSpacing="10"
                          Padding="2"
                          BackgroundColor="#333333">

                        <Label Text="{Binding Value.Columns[0].ColumnName}" Grid.Column="0" FontAttributes="Bold" FontSize="{Binding FontSizeForCaption}"  Style="{DynamicResource StatLabelStyle}"/>
                        <Label Text="{Binding Value.Columns[1].ColumnName}" Grid.Column="1" FontAttributes="Bold" FontSize="{Binding FontSizeForCaption}"  Style="{DynamicResource StatLabelStyle}"/>
                        <Label Text="{Binding Value.Columns[2].ColumnName}" Grid.Column="2" FontAttributes="Bold" FontSize="{Binding FontSizeForCaption}"  Style="{DynamicResource StatLabelStyle}"/>
                        <Label Text="{Binding Value.Columns[3].ColumnName}" Grid.Column="3" FontAttributes="Bold" FontSize="{Binding FontSizeForCaption}"  Style="{DynamicResource StatLabelStyle}"/>

                    </Grid>

                    <!-- ROWS -->
                    <CollectionView ItemsSource="{Binding Value.Rows}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*,*,*,*"
                                      ColumnSpacing="10"
                                      Padding="2">

                                    <Label Text="{Binding [0]}" Grid.Column="0" FontSize="{Binding FontSizeForLabel}"  Style="{DynamicResource StatLabelStyle}"/>
                                    <Label Text="{Binding [1]}" Grid.Column="1" FontSize="{Binding FontSizeForLabel}"  Style="{DynamicResource StatLabelStyle}"/>
                                    <Label Text="{Binding [2]}" Grid.Column="2" FontSize="{Binding FontSizeForLabel}"  Style="{DynamicResource StatLabelStyle}"/>
                                    <Label Text="{Binding [3]}" Grid.Column="3" FontSize="{Binding FontSizeForLabel}"  Style="{DynamicResource StatLabelStyle}"/>

                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                </VerticalStackLayout>
            </DataTemplate>

            <!-- TEMPLATE SELECTOR -->
            <local:StatValueTemplateSelector
            x:Key="StatSelector"
            TextTemplate="{StaticResource TextTemplate}"
            BoolTemplate="{StaticResource BoolTemplate}"
            TableTemplate="{StaticResource TableTemplate}" />

        </ResourceDictionary>
    </ContentPage.Resources>

    <AbsoluteLayout BackgroundColor="Black">

        <BoxView Style="{DynamicResource SeparatorBoxViewStyle}" x:Name="PositionBoxView1"/>

        <skia:SKCanvasView AbsoluteLayout.LayoutBounds="0.5,0.5,1.0,1.0" AbsoluteLayout.LayoutFlags="All"
            IsVisible="True"
            x:Name="SpectrumCanvas"
            PaintSurface="OnPaintSurface" />

        <CollectionView  AbsoluteLayout.LayoutBounds="0.5,0.5,0.95,0.95" AbsoluteLayout.LayoutFlags="All" BackgroundColor="Transparent"
        ItemsSource="{Binding Stats}"
        ItemTemplate="{StaticResource StatSelector}" />

        <BoxView Style="{DynamicResource SeparatorBoxViewStyle}" x:Name="PositionBoxView2"/>

    </AbsoluteLayout>

</ContentPage>